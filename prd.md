# Grasshopper AI助手demo

## 1. 产品战略

### 1.1 用户画像

| 维度 | 描述 |
|------|------|
| **角色** | 参数化BIM建模工程师 |
| **技能水平** | 熟练使用Rhino + Grasshopper，但插件生态复杂，学习曲线陡峭 |
| **核心痛点** | ① 电池数量大（原生300+，插件更多），查找耗时；② 思维断层：设计师脑中有一个清晰的"几何最终形态"，但不知道如何将其"翻译"成Grasshopper的"数据流逻辑"（树形结构、相对坐标、向量方向）；③ 路径依赖陷阱：设计师通常会从自己最熟悉的几个电池开始堆叠，导致电池组臃肿（运算量巨大）或后续修改时牵一发而动全身（参数化特征丢失） |
| **现有方案** | 官方文档（英文、零散）、论坛搜索（效率低）、同事请教（不可持续） |
| **典型场景** | 面对模糊的高阶建模需求，期望AI提供从宏观步骤拆解到微观数据流控制的完整解题思路 |

### 1.2 产品定位

**"不是通用Chatbot，而是拆解复杂需求、精准回答问题的引擎"**

与通用LLM的差异：

| 维度 | 通用AI | 本产品 |
|------|--------|--------|
| 电池知识 | 版本可能过时，电池输入和输出结构易错 | 基于电池文档 + QA的RAG检索 |
| 问题理解 | 需要用户提供完整上下文 | 7维关键词提取，自动识别幕墙领域概念 |
| 回答格式 | 自由文本 | 结构化Markdown：电池链图解 + 步骤表 + 常见陷阱 |
| 故障排查 | 通用建议 | 按任务类型路由到专用诊断模板，含排查步骤 |
| 知识溯源 | 无 | 引用来源可追溯至具体电池文档/手册章节 |

---

## 2. 核心能力

| 能力域 | 功能 | 产品价值 |
|--------|------|----------|
| **智能问答** | 4种任务类型分类（**故障诊断**/**知识查询**/**搭建指导**/**数据处理**） | 不同任务使用不同回答策略，而非"一刀切" |
| | 7维关键词提取（构件/几何/插件/操作/数据/现象/幕墙类型） | 通过问题丰富（enrich）优化用户提问，解析用户意图，驱动后续检索与回答 |
| | 交互式追问（信息不足时主动澄清） | 防止用户输入内容不全面，自动交互补齐，预防输出幻觉和出错 |
| **知识检索** | 双知识库（Grasshopper电池 + Rhino 8官方手册） | 覆盖电池行为与Rhino原生操作两大知识域 |
| | 差异化知识处理策略：电池库LLM元数据增强 + 手册库PDF书签层级切分 | 针对数据形态差异（结构化JSON vs 非结构化PDF）采用不同处理管线，让两类知识各取所长，检索更精准高效 |
| | 分层检索策略（精确匹配→向量检索→重排序） | 已知电池名<50ms直达，模糊描述走向量检索 |
| | 按场景差异化检索配置 | 知识查询偏效率，故障诊断偏全面 |
| **回答生成** | 结构化Markdown输出（电池链图解+步骤+陷阱） | 用户可按步骤搭建电池工作流 |
| | 流式输出 + 思维链推理过程展示 | 增加用户交互体验，推理过程可独立折叠 |
| **可观测性** | 检索链路追踪（各阶段耗时、匹配模式、文档数） | 量化检索效果，驱动参数调优 |
| | 对话链路追踪（token消耗、响应时间） | 成本可控，性能可度量 |

---

## 3. 关键设计

### 3.1 任务分类驱动的提示词路由

**决策**：将用户问题分为4种任务类型，每种类型对应一套独立的提示词模板。

**取舍逻辑**：不同任务对回答的要求完全不同

- **知识查询**要求快速、精准，给电池名+面板位置+输入输出+最简电池链示例，一句话就能解决的不展开，追求极致的回答速度；
- **故障诊断**要求全面排查，区分"GH电池报错"（只能给原因和检查方向，不给电池链）和"Rhino环境异常"（给Rhino命令和操作步骤），避免误导用户；
- **搭建指导**要求最全面、最准确的回答——从宏观步骤拆解到微观数据流控制，必须输出电池链图解+详细步骤表+常见陷阱+插件信息，不遗漏任何搭建细节；
- **数据处理**要求关注数据树/列表操作的正确性，区分"纯数据结构操作""非几何属性管理""文件导出与格式管理"三类子场景，说明处理前后的数据结构变化，确保最终bake时数据树结构一致。

### 3.2 提示词工程

1. **内部验证机制**：搭建指导和通用分析模板要求LLM在输出前执行"思维树探索→最优路径筛选→最终校验"三步内部验证——从数据类型兼容、数据结构匹配、路径简洁度、插件冲突四个维度对比备选方案，校验不通过则修正重试。

2. **Few-shot案例样本**：故障诊断、知识查询、数据处理模板各含2个真实案例样本（用户输入→完整回答）。

3. **子场景路由**：知识查询模板内部区分"GH电池查询"和"Rhino操作查询"两种子场景，回答风格完全不同（前者给电池名+面板+电池链，后者给操作路径+Rhino命令+步骤）。数据处理模板内部区分"纯数据结构""属性管理""文件导出"三类子场景。这种子场景路由让同一模板适配不同粒度的问题。

4. **分类规则强制执行**：意图理解Prompt中通过测试总结出多组分类规则，通过优先级+强信号词锚定避免LLM误判。*（例如"如果用户输入包含'排序''数据树''编号'等词汇，无论操作对象是几何体还是数据，都优先归类为数据处理"；"如果用户描述中包含任何几何生成动作（挤出、切割、偏移等），且该动作为核心诉求，则归类为搭建指导"；"如果用户描述的是Rhino环境中的异常（卡顿、布尔失败、物件飞散），应归类为故障诊断而非知识查询"。）* 这些强制规则须在测试和使用过程中优化修正，目前已满足正常使用的准确性。

5. **核心约束一致性**：所有模板共享三条硬约束——优先原生电池+Elefront_518、几何生成禁止输出Mesh、需要手动操作时直接说明无需等待用户确认。这三条约束贯穿所有模板，确保回答风格一致可控。

### 3.3 意图理解：一次LLM调用完成四个任务

**决策**：将任务分类、关键词提取、检索目标判断、问题Enrich合并为一次LLM调用。

**原始方案**：分三次调用（分类→关键词→检索目标）。

**取舍逻辑**：分三次调用意味着3倍LLM延迟和3倍Token消耗，而实测发现合并为一次调用后，分类和关键词提取的准确度变化不大。对于复杂易错的任务分类（如"数据树排序"应归为数据处理而非搭建指导），通过精细的Prompt工程（分类规则+优先级+强信号词锚定）已经完全避免了误判。这是一次"成本驱动"的取舍——在保证准确度的前提下，将延迟和成本降到最低。

### 3.4 检索策略：按信息确定性分层

**决策**：不统一用向量检索，而是按用户对知识对象的确定性选择不同路径——精确匹配（<50ms）、向量检索（~1s）、Fallback全量召回（~1.5s）。

**取舍逻辑**：已知电池名的查询（如"Isotrim怎么用"）走精确匹配，约80%的关于电池用法的查询都能命中这条快车道。模糊描述走向量检索，首次检索失败才触发Fallback。整体平均延迟控制在1s内——如果全部走向量检索，延迟会稳定在~1s，但丢掉了精确匹配<50ms的优势。

**精确匹配的触发条件**：精确匹配需要同时满足三个条件——① 检索目标为 `battery_only`（意图理解阶段判定问题仅涉及电池知识）；② 目录预过滤成功匹配到具体的电池条目；③ 匹配到的电池名存在于底账中。*（三个条件缺一不可，任何一条不满足则自动降级为向量检索。）*

### 3.5 场景驱动的检索参数差异化

**决策**：知识查询场景减少召回（battery_top_k=8），故障诊断场景增大召回（battery_top_k=15）。

**取舍逻辑**：知识查询用户期望快速得到精准答案，少召回=低延迟；故障诊断用户愿意多等几秒换取全面排查，多召回=高覆盖。

### 3.6 RAG源文档的切分原则设计

**决策**：电池库（结构化JSON）用LLM元数据增强+精确过滤，手册库（非结构化PDF）用书签层级切分保持上下文。

**取舍逻辑**：电池库有明确的 `battery_name`、`category`、`menu_path` 等字段，结构化程度高，适合用元数据做精确过滤。手册库是线性PDF，章节边界清晰，才能用书签驱动的层级切分保持"操作步骤"的上下文完整性。

---

## 4. 迭代计划

### 4.1 近期（1-2周）：快速提升体验，改动小、价值高

| 方向 | 说明 | 落地要点 |
|------|------|----------|
| **回复溯源** | 回答底部展示引用来源，可点击查看原文 | 提升透明度和信任度 |
| **上下文窗口管理** | 滑动窗口截断 + 摘要压缩，防止长对话 Token 溢出 | 当前 `get_chat_history` 已加载历史，加阈值检查和压缩策略即可 |

### 4.2 中期（1-2月）：让知识库具备学习能力

| 方向 | 说明 | 可行性评估 |
|------|------|-----------|
| **知识库增量更新** | 支持用户上传文件，加入用户对应的RAG数据库和检索库中，针对不同文件采取不同解析和分块方式 | ✅ 高。底账为 JSONL 支持追加写入，Chroma 支持 `add_texts` |
| **用户收藏与反馈** | 前端"收藏"按钮 → 将回答写入 `collected.jsonl` → 定期合并入知识库，重复性类似性的电池搭建要求可直接作为上下文参考调用；同时点赞/点踩数据驱动知识库优化和后续模型微调 | ✅ 高。收藏 + 反馈形成完整的用户数据流，需设计收藏数据与原始数据的分离管理以及版本回溯 |
| **电池组 JSONL 导入** | 用户导出 Grasshopper 电池组为 JSONL，系统解析为结构化的电池连接图描述，作为额外上下文注入 | ✅ 中高。需设计 JSONL 解析逻辑（节点+连线），注意大电池组可能超出上下文限制 |

### 4.3 远期（3月+）：差异化能力建设

| 方向 | 说明 | 可行性评估 |
|------|------|-----------|
| **多模态图像识别** | 上传电池连线图截图，视觉模型理解电池组连接逻辑 | ⚠️ 中。依赖多模态模型对 Grasshopper 电池图标的识别效果，需先验证模型能力再全量开发 |
| **Agent 功能** | 系统自主决策调用工具辅助完成搭建任务。目前缺少可直接操作 Grasshopper 的插件，通过 JSONL 作为数据源自行开发插件难度较大，需先明确技术可行路径再评估投入 | ⚠️ 中低。需引入多轮工具调用循环、执行环境安全隔离、错误恢复机制，架构改动较大 |